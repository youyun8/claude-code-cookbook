name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /claude-code-cookbook
          NEXT_PUBLIC_SITE_URL: https://youyun8.github.io/claude-code-cookbook

      - name: Upload website
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@v4

import type { Lesson } from '../types';

export const lesson: Lesson = {
  slug: 'project-instructions-claude-md',
  moduleNumber: 5,
  title: 'Project instructions with CLAUDE.md',
  summary:
    'What belongs in durable project instructions, what does not, and why a long instruction file makes every rule in it less likely to be followed.',
  level: 'Daily workflow',
  minutes: 30,
  prerequisites: ['context-and-session-management'],
  objectives: [
    'Explain what CLAUDE.md is, where it lives, and when it loads.',
    'Decide what to write down and what to leave out.',
    'Recognise when a rule needs a hook instead of an instruction.',
    'Prune an instruction file that has grown past usefulness.',
  ],
  blocks: [
    { kind: 'h2', id: 'what', text: 'What it is' },
    {
      kind: 'p',
      text: 'A `CLAUDE.md` file is markdown you write to give Claude persistent instructions for a project. It is loaded into the context window at the start of every session, before your first message.',
    },
    {
      kind: 'p',
      text: 'The single most important property: it is **context, not configuration**. Claude reads it and tries to follow it. Nothing enforces it. A rule you cannot afford to have skipped does not belong here — it belongs in a hook or a permission rule, both covered later.',
    },
    {
      kind: 'table',
      caption: 'Where instruction files live, from broadest to most specific',
      head: ['Scope', 'Location', 'Shared with'],
      rows: [
        [
          'Managed policy',
          'An OS-specific managed path deployed by IT',
          'Everyone in the organisation',
        ],
        ['User', '`~/.claude/CLAUDE.md`', 'Just you, across all projects'],
        ['Project', '`./CLAUDE.md` or `./.claude/CLAUDE.md`', 'Your team, via version control'],
        ['Local', '`./CLAUDE.local.md`', 'Just you, this project — gitignore it'],
      ],
    },
    {
      kind: 'p',
      text: 'Files are discovered from your working directory upward and concatenated rather than overriding each other. Files in subdirectories load on demand when Claude reads files in those directories. Run `/context` in a session and look under **Memory files** to confirm what actually loaded — if a file is not listed there, Claude cannot see it.',
    },
    { kind: 'sources', ids: ['memory'] },

    { kind: 'h2', id: 'start', text: 'Getting a first one' },
    {
      kind: 'p',
      text: 'Run `/init` and Claude analyses the codebase and writes a starting `CLAUDE.md` with the build commands, test instructions and conventions it can discover. If one already exists, `/init` suggests improvements rather than overwriting it.',
    },
    {
      kind: 'p',
      text: 'Treat the generated file as a draft, not a result. The parts worth keeping are the ones Claude could not have discovered — and by construction, `/init` wrote the discoverable parts.',
    },

    { kind: 'h2', id: 'include', text: 'What to write down' },
    {
      kind: 'p',
      text: 'One test decides most cases: **would a new teammate need this explained, and would they still need it explained next month?**',
    },
    {
      kind: 'ul',
      items: [
        '**Non-obvious commands.** `npm run test:integration` needs Docker running. The type check is `npm run check`, not `tsc`. The dev server needs `--turbo` or it recompiles the world.',
        '**Conventions that differ from the tool defaults.** Where a reasonable engineer would guess wrong.',
        '**Verification requirements.** "Changes under `src/api/` require `npm run test:contract` to pass."',
        '**Architecture decisions with rationale.** "Handlers must stay free of database calls; the repository layer exists for that." The rationale is what makes it applicable to a new case.',
        '**Repository etiquette.** Branch naming, commit conventions, whether to commit at all.',
        '**Environment quirks.** The thing that wastes twenty minutes of every new hire’s first week.',
      ],
    },
    {
      kind: 'code',
      lang: 'markdown',
      label: 'A short, useful CLAUDE.md',
      code: `# Payments service

## Commands
- Test: \`npm test\` — integration tests need \`docker compose up -d db\` first
- Type check: \`npm run check\` (not \`tsc\`; we pass project references)
- Lint: \`npm run lint -- --fix\`

## Conventions
- Money is \`Cents\` (integer). Never use floats for currency.
- HTTP handlers must not call the database directly; go through \`src/repo/\`.
- New endpoints need a contract test in \`tests/contract/\`.

## Before you finish
- Run \`npm run check\` and the tests for the area you touched.
- Do not edit \`src/generated/\` — it is regenerated by \`npm run codegen\`.`,
    },

    { kind: 'h2', id: 'exclude', text: 'What to leave out' },
    {
      kind: 'table',
      head: ['Do not include', 'Why', 'Where it belongs'],
      rows: [
        [
          '"Write clean code", "follow best practices"',
          'Changes nothing, and dilutes the rules around it',
          'Nowhere',
        ],
        [
          'Directory listings and dependency lists',
          'Claude can read them, and your copy goes stale',
          'The repository itself',
        ],
        [
          '"We are currently migrating to v2"',
          'It will be wrong in a month and nobody will fix it',
          'The issue tracker',
        ],
        [
          'A long onboarding tutorial',
          'Costs context on every task, relevant to almost none',
          'A skill, or the README',
        ],
        [
          'File-by-file descriptions',
          'Enormous, stale within weeks',
          'Code, tests, and docstrings',
        ],
        [
          '"Always run the formatter after editing"',
          'It is a guarantee you need, not a preference',
          'A `PostToolUse` hook',
        ],
      ],
    },
    {
      kind: 'callout',
      tone: 'warning',
      title: 'Length has a cost you cannot see',
      body: [
        'The documented target is under 200 lines. Longer files consume more context every session and reduce how reliably any individual rule is followed.',
        'This is the counter-intuitive part: adding a rule to a long file can make the existing rules less effective. Pruning is a feature, not housekeeping.',
        'For large projects, move path-specific guidance into `.claude/rules/` files with `paths:` frontmatter so they load only when Claude touches matching files.',
      ],
    },
    {
      kind: 'lab',
      lab: 'claude-md-linter',
      title: 'CLAUDE.md linter',
      description:
        'Paste a file, or load a deliberately bad sample, and see heuristic findings with suggested rewrites.',
    },

    { kind: 'h2', id: 'specific', text: 'Write rules you could check' },
    {
      kind: 'compare',
      title: 'Same intent, different reliability',
      sides: [
        {
          label: 'Vague',
          tone: 'weak',
          code: `- Format code properly
- Test your changes
- Keep files organized
- Handle errors appropriately`,
          notes: [
            'No way to tell whether any of these was followed.',
            'Every line is true of every codebase, so none of them says anything about yours.',
            'Hedged language leaves room to skip the instruction.',
          ],
        },
        {
          label: 'Specific',
          tone: 'strong',
          code: `- Use 2-space indentation
- Run \`npm test\` before committing
- API handlers live in \`src/api/handlers/\`
- Wrap external calls in \`withRetry()\` from \`src/lib/retry.ts\``,
          notes: [
            'Each line is concrete enough to verify by looking.',
            'Each one is specific to this repository.',
            'Named paths and functions give Claude somewhere to go.',
          ],
        },
      ],
    },
    {
      kind: 'p',
      text: 'Consistency matters too. If two instructions contradict each other — across your project file, a nested one, and `.claude/rules/` — Claude may pick either. Review periodically and delete the loser.',
    },

    { kind: 'h2', id: 'contrast', text: 'Instructions, skills, and hooks' },
    {
      kind: 'p',
      text: 'These three are easy to confuse and the distinction is load-bearing. Module 7 goes deeper; the short version:',
    },
    {
      kind: 'table',
      head: ['Mechanism', 'Loads', 'Guarantee', 'Use for'],
      rows: [
        [
          'CLAUDE.md',
          'Every session',
          'None — it is context',
          'Facts and rules that apply broadly',
        ],
        [
          'Skill',
          'When invoked or judged relevant',
          'None — it is context',
          'Multi-step procedures used occasionally',
        ],
        [
          'Hook',
          'At a fixed lifecycle event',
          'Runs regardless of what Claude decides',
          'Things that must happen every time',
        ],
      ],
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'The deciding question',
      body: [
        'Ask: "if Claude skipped this, would I be annoyed, or would something break?"',
        'Annoyed → CLAUDE.md. Broken → hook. A long procedure you need occasionally → skill.',
      ],
    },
    { kind: 'sources', ids: ['memory', 'skills', 'hooksGuide', 'featuresOverview'] },

    { kind: 'h2', id: 'maintain', text: 'Maintain it like code' },
    {
      kind: 'p',
      text: 'Add a line when Claude makes the same mistake twice, when a code review catches something it should have known, or when you type the same correction you typed last session. Delete a line when it is no longer true, when it was never specific, or when the file has grown past the point where you would read it yourself.',
    },
    {
      kind: 'p',
      text: 'If Claude is not following an instruction: check `/context` to confirm the file loaded, make the instruction more specific, look for a contradicting rule elsewhere — and if it must happen every time, stop trying to phrase it better and write a hook.',
    },
  ],
  checklist: [
    'My CLAUDE.md is under about 200 lines.',
    'Every line is specific to this repository.',
    'Nothing in it is discoverable from the code itself.',
    'Nothing in it will be false in a month.',
    'Rules that must always run are hooks, not sentences.',
    'I confirmed with `/context` that the file actually loaded.',
    'No two rules contradict each other.',
  ],
  quiz: [
    {
      id: 'q1',
      prompt: 'Why can a longer CLAUDE.md make Claude follow your rules less reliably?',
      options: [
        { id: 'a', text: 'Claude Code truncates files over a fixed line count.' },
        {
          id: 'b',
          text: 'It consumes more context every session and dilutes the important rules.',
        },
        { id: 'c', text: 'Long files load more slowly.' },
        { id: 'd', text: 'It does not — longer is always better.' },
      ],
      correctOptionId: 'b',
      explanation:
        'The file is loaded into every session. More lines means more context spent and each individual rule competing with more surrounding text.',
    },
    {
      id: 'q2',
      prompt:
        'You need the formatter to run after every single edit, without exception. Where does that belong?',
      options: [
        { id: 'a', text: 'CLAUDE.md, phrased emphatically.' },
        { id: 'b', text: 'A PostToolUse hook.' },
        { id: 'c', text: 'A skill invoked at the end of each session.' },
        { id: 'd', text: 'The README.' },
      ],
      correctOptionId: 'b',
      explanation:
        'CLAUDE.md is context, not enforcement. A hook executes at a fixed lifecycle event regardless of what Claude decides, which is exactly what "without exception" requires.',
    },
    {
      id: 'q3',
      prompt: 'Which of these belongs in a project CLAUDE.md?',
      options: [
        { id: 'a', text: 'A list of the project’s dependencies and their versions.' },
        { id: 'b', text: '"Integration tests need `docker compose up -d db` first."' },
        { id: 'c', text: '"We are migrating to the new API this quarter."' },
        { id: 'd', text: '"Write readable, maintainable code."' },
      ],
      correctOptionId: 'b',
      explanation:
        'It is non-obvious, specific, stable, and it saves a failed test run every time. The others are discoverable, transient, or generic.',
    },
    {
      id: 'q4',
      prompt:
        'An instruction in CLAUDE.md is being ignored. What is the least useful first response?',
      options: [
        { id: 'a', text: 'Run `/context` to confirm the file loaded.' },
        { id: 'b', text: 'Make the instruction concrete and checkable.' },
        { id: 'c', text: 'Add "IMPORTANT" and repeat the rule three times.' },
        { id: 'd', text: 'Look for a contradicting rule in another instruction file.' },
      ],
      correctOptionId: 'c',
      explanation:
        'Repetition adds context without adding clarity. Verify it loaded, make it specific, remove contradictions — and if it must always happen, use a hook.',
    },
  ],
  sources: ['memory', 'skills', 'hooksGuide', 'featuresOverview', 'largeCodebases'],
};
